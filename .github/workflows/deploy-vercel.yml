
name: Trigger Vercel Build
on:
  push:
    branches:
      - master
      - editorial_workflow

jobs:
  build:
    name: Request Vercel Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -X POST -d {} ${{ secrets.VERCEL_BUILD_HOOK }}
